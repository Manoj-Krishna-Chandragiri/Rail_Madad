# ============================================================
# EC2 Production Requirements (t2.micro compatible)
# Heavy AI packages (tensorflow, deepface) excluded:
#   - t2.micro only has 8GB disk + 1GB RAM
#   - Gemini AI (cloud API) handles multimodal features
#   - DeepFace/local BERT disabled in production via env var
# ============================================================

# Core Django Framework
Django==5.1.5
djangorestframework==3.15.2

# CORS and Security
django-cors-headers==4.6.0
whitenoise>=6.0.0

# Authentication and Firebase
firebase-admin==6.7.0

# Environment Configuration
python-dotenv==1.0.1

# Image Processing and File Handling
Pillow==11.1.0

# HTTP Requests
requests==2.31.0

# Translation
googletrans==4.0.0rc1

# Database
mysqlclient==2.2.4

# Production Server
gunicorn==23.0.0

# Lightweight ML (no tensorflow/torch)
scikit-learn==1.5.1
pandas==2.1.4
numpy==1.24.3
joblib==1.3.2

# Gemini AI for Multimodal Analysis (cloud API, no local model)
google-generativeai==0.8.3

# Cloudinary for Media Storage
cloudinary==1.41.0

# NOTE: deepface / tf-keras / tensorflow intentionally excluded
# Face authentication endpoints will return a graceful error in production
# until a lightweight alternative is deployed (e.g. on a larger instance)
